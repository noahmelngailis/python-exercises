def is_prime(num):
  if num <= 1:
      return False
  else:
      for n in range(2, num):
          if num % n == 0:
              return False
          else:
              return True

def is_prime(num):
  if num <= 1:
      return False
  else:
      for n in range(2, num):
          if num % n == 0:
              return False
          else:
              return True

def duplicate_count(text):
    text = text.lower()
    answer = len(text) - len(set(text)) 
    set_idea = text.strip(set(text)) 
    return set_idea     



  